)};=>
function() = bellringer()
ping-sweep(localhost).print=>array[].obj.ambience;

function() = humn()
.get.IPconfigs
.get.DirPerms
.get.Globals
.get.Locals
set humn() to array

function() = hymn()
usocket.humn()=>.bellringer()=>ndarray[].obj.
function export hymn();

function() = sing()
require "raver" from rave;
require "crowd" from sample;
require ''underscore" from underscore;
require "lowDB" from *;
require "lodash" from:

let raver infect crowd;
//do stuff
/// ndarray type workflow code with lowDB, underscore and lodash that makes all the lies true and the magic seem real.

Export chant.js as chant();
